[tool.pytest.ini_options]
testpaths = ["server/gateway/tests"]
pythonpath = ["server/gateway"]
addopts = "-ra"

[tool.ruff]
target-version = "py311"
line-length = 100
src = ["server/gateway"]

[tool.ruff.lint]
select = ["E", "F", "I", "B", "ASYNC"]
