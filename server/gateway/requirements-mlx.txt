# Apple Silicon GPU transcription through Apple's MLX/Metal framework.
# Kept separate so Linux CPU and Windows CUDA packages never install MLX.
mlx-whisper>=0.4.2; sys_platform == "darwin" and platform_machine == "arm64"
